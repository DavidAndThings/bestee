_PERSONS_DIR_ENV = "BESTEE_PERSONS_DIR"
_DEFAULT_PERSONS_DIR = "resources/persons"

# Default maximum size of a single JSON file before records spill into a new one.
MAX_FILE_BYTES = 64 * 1024
EXCHANGE_REJECTION_THRESHOLD = 0.5
